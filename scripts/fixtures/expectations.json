{
  "scenarios": [
    { "settings": ["pi-settings-ok-root.json"], "expect": "accepted" },
    { "settings": ["pi-settings-ok-pi-lite.json"], "expect": "accepted" },
    { "settings": ["pi-settings-refused-dual-pstack.json"], "expect": "refused" },
    { "settings": ["pi-settings-refused-local-paths.json"], "expect": "refused" },
    { "settings": ["pi-settings-ok-root.json", "pi-settings-ok-pi-lite.json"], "expect": "refused" }
  ]
}
